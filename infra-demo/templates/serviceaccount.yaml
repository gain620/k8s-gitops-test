{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "infra-demo.serviceAccountName" . }}
  labels:
    {{- include "infra-demo.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  namespace: {{ .Values.namespace }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
